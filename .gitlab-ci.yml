cache:
  paths:
    - node_modules/

stages:
  - build

before_script:
  - apt-get update
  - apt-get install -y nodejs
  

build:
  stage: build
  before_script: 
    - npm install
  script:
    - node ./node_modules/aurelia-cli/bin/aurelia-cli.js build --env prod